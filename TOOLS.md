#!/usr/bin/env markdown
# TOOLS.md (Maintainer Lane)

Hard rule: no secrets in markdown files.

Workspace:
- `/Users/anima/repos/openclaw-core`

Tool posture:
- Use `gateway`, `group:runtime`, `group:fs`, and `group:sessions` for maintenance work.
- Prefer host-compatible deterministic commands.
- Avoid broad web browsing from this lane.

Evidence requirement:
- For each maintenance action, include command, key output, and affected file path.
