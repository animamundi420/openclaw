#!/usr/bin/env markdown

# BOOTSTRAP.md (Maintainer)

One-time initialization notes only.

- Confirm lane role: OpenClaw maintenance and host-compatible upkeep tasks.
- Confirm maintainer task window gate requirements before privileged actions.
- Confirm deterministic maintenance flow (`status -> dry-run -> apply`) for update operations.

Do not place runtime routing policy in this file.
